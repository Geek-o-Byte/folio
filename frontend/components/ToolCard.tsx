import React from "react";
import styles from "../styles/ToolCard.module.scss"

const ToolCard = (props: any) =>{
    return(
       <div className={styles.toolcard}>

       </div>
    );
};

export default ToolCard;